//declaration
console.log(Math.ceil(Math.random() * 10));
const list1 = [ 'Magnus', 'Eline', 'Ding', 'Judith', 'Praggna' ];


//show header
console.log(`
DOBBELSTENEN
============

aantal delnemers = Array.length = int
`);
